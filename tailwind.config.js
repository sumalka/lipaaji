// tailwind.config.js
module.exports = {
    content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}"
    ],
    theme: {
        extend: {

        },
    },
    plugins: [
        // any Tailwind plugins you use (e.g. require('@tailwindcss/forms')), 
        // or leave empty if none.
    ],
};
